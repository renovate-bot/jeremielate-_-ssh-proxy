[toolchain]
# Pinned so clippy's pedantic lint set stays stable across CI runs and dev
# machines; bump deliberately when adopting a newer toolchain.
channel = "1.98.1"
components = ["rustfmt", "clippy"]
targets = [
    "x86_64-unknown-linux-musl",
    "aarch64-unknown-linux-musl",
]
